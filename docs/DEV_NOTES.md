# Dev Notes

## Repo Discovery (Phase 0)
- Entry file: `src/script.js` imported by `src/index.html`
- Build tool: Vite (`vite.config.js`)
- TypeScript: No (plain JavaScript, ESM)
- Rings/text rendering: `src/script.js` uses `TextGeometry` + torus meshes
- GUI/controls: `lil-gui` wireframe toggle, `OrbitControls` for camera
